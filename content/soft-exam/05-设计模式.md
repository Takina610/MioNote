![](./img/Snipaste_2025-10-15_20-17-58.png)

- **终结者模式改为中介者模式**
- **生成器模式也可以叫创建者模式**

![](./img/Snipaste_2025-10-15_20-21-07.png)

# 创建型模式

## 简单工厂模式，考

![](./img/Snipaste_2025-10-15_20-30-56.png)

==**简单工厂模式不符合开闭原则（**对扩展开放，对修改关闭**。在程序需要进行拓展的时候，不能去修改原有的代码，实现一个热插拔的效果）**==

```java
public class Client {
    public static void main(String[] args) {
        Product productA = SimpleFactory.create("A");
        productA.info();

        Product productB = SimpleFactory.create("B");
        productB.info();
    }
}

class SimpleFactory {
    public static Product create(String type) {
        if (type.equals("A")) {
            return new ProductA();
        } else if (type.equals("B")) {
            return new ProductB();
        } else {
            return null;
        }
    }
}

abstract class Product {
    public abstract void info();
}

class ProductA extends Product{
    @Override
    public void info() {
        System.out.println("产品A");
    }
}

class ProductB extends Product{
    @Override
    public void info() {
        System.out.println("产品B");
    }
}
```

## 工厂方法模式(类)

![](./img/Snipaste_2025-10-15_20-49-28.png)

```java
public class Client {
    public static void main(String[] args) {
        Factory factory = new FactoryA();

        Product productA = factory.create();
        productA.info();

        factory = new FactoryB();
        Product productB = factory.create();
        productB.info();
    }
}

interface Factory {
    Product create();
}

class FactoryA implements Factory{
    @Override
    public Product create() {
        return new ProductA();
    }
}

class FactoryB implements Factory{
    @Override
    public Product create() {
        return new ProductB();
    }
}

interface Product {
    void info();
}

class ProductA implements Product {
    @Override
    public void info() {
        System.out.println("产品A");
    }
}

class ProductB implements Product {
    @Override
    public void info() {
        System.out.println("产品B");
    }
}
```

## 抽象工厂模式

![](./img/Snipaste_2025-10-15_21-09-05.png)

```java
public class Client {
    public static void main(String[] args) {
        AbstractFactory factory = new Factory1();
        AbstractProductA productA1 = factory.createProductA();
        AbstractProductB productB1 = factory.createProductB();
        productA1.info(); productB1.info();

        factory = new Factory2();
        AbstractProductA productA2 = factory.createProductA();
        AbstractProductB productB2 = factory.createProductB();
        productA2.info(); productB2.info();
    }
}

public interface AbstractFactory {
    AbstractProductA createProductA();
    AbstractProductB createProductB();
}

class Factory1 implements AbstractFactory{
    @Override
    public AbstractProductA createProductA() {
        return new ProductA1();
    }

    @Override
    public AbstractProductB createProductB() {
        return new ProductB1();
    }
}

class Factory2 implements AbstractFactory{
    @Override
    public AbstractProductA createProductA() {
        return new ProductA2();
    }

    @Override
    public AbstractProductB createProductB() {
        return new ProductB2();
    }
}

public interface AbstractProductA {
    void info();
}

class ProductA1 implements AbstractProductA{
    @Override
    public void info() {
        System.out.println("产品A1");
    }
}

class ProductA2 implements AbstractProductA{
    @Override
    public void info() {
        System.out.println("产品A2");
    }
}

public interface AbstractProductB {
    void info();
}

class ProductB1 implements AbstractProductB{
    @Override
    public void info() {
        System.out.println("产品B1");
    }
}

class ProductB2 implements AbstractProductB{
    @Override
    public void info() {
        System.out.println("产品B2");
    }
}
```

![](./img/Snipaste_2025-10-16_15-40-32.png)

![](./img/Snipaste_2025-10-16_15-41-00.png)

## 建造者模式(生成器模式)，考

![](./img/Snipaste_2025-10-15_21-23-36.png)

```java
public class Client {
    public static void main(String[] args) {
        Builder builder1 = new Builder1();

        AbstractDirector director = new Director();
        director.construct(builder1);

        AbstractProduct product1 = builder1.getResult();
        product1.show();

        Builder builder2 = new Builder2();
        director.construct(builder2);

        AbstractProduct product2 = builder2.getResult();
        product2.show();
    }
}

public interface AbstractDirector {
    void construct(Builder builder);
}

class Director implements AbstractDirector{
    @Override
    public void construct(Builder builder) {
        builder.buildPart();
    }
}

public interface Builder {
    void buildPart();
    AbstractProduct getResult();
}

class Builder1 implements Builder{
    AbstractProduct product = new Product();

    @Override
    public void buildPart() {
        product.add("A");
        product.add("B");
    }

    @Override
    public AbstractProduct getResult() {
        return product;
    }
}

class Builder2 implements Builder{
    AbstractProduct product = new Product();

    @Override
    public void buildPart() {
        product.add("A");
        product.add("B");
        product.add("C");
    }

    @Override
    public AbstractProduct getResult() {
        return product;
    }
}

public interface AbstractProduct {
    void add(String part);
    void show();
}

class Product implements AbstractProduct {
    List<String> product = new ArrayList<>();

    @Override
    public void add(String part) {
        product.add(part);
    }

    @Override
    public void show() {
        for (String part: product) {
            System.out.print(part);
        }
        System.out.println();
    }
}
```

![](./img/Snipaste_2025-10-15_21-47-25.png)

![](./img/Snipaste_2025-10-16_15-43-03.png)

## 原型模式

![](./img/Snipaste_2025-10-16_11-58-04.png)

```java
public class Client {
    public static void main(String[] args) {
        Product product1 = new Product("11", 22);
        System.out.println(product1);

        Product product2 = (Product) product1.clone();
        System.out.println(product2 == product1);
    }
}

public interface Prototype {
    Object clone();
}

class Product implements Prototype {
    private String name;
    private int price;

    public Product() {
    }

    public Product(String name, int price) {
        this.name = name;
        this.price = price;
    }

    @Override
    public String toString() {
        return "Product{" +
                "name='" + name + '\'' +
                ", price=" + price +
                '}';
    }

    @Override
    public Object clone() {
        Product product = new Product();
        product.name = this.name;
        product.price = this.price;
        return product;
    }
}
```

## 单例模式

![](./img/Snipaste_2025-10-16_12-07-55.png)

```java
public class Client {
    public static void main(String[] args) {
        Singleton instance1 = Singleton.getInstance();
        Singleton instance2 = Singleton.getInstance();
        System.out.println(instance1 == instance2);
    }
}

public class Singleton {
    private static final Singleton singleton = new Singleton();

    private Singleton() {}

    public static Singleton getInstance() {
        return singleton;
    }
}
```

# 结构型模式

## 适配器模式(1次考过)(类，对象)

![](./img/Snipaste_2025-10-16_15-29-10.png)

![](./img/Snipaste_2025-10-16_15-29-36.png)

```java
public class Client {
    public static void main(String[] args) {
        USB usb = new Adapter();
        usb.request();
    }
}

// ? Target
class USB {
    public void request() {
        System.out.println("USB数据线");
    }
}

// ? Adapter
public class Adapter extends USB{
    TypeC typeC = new TypeC();

    @Override
    public void request() {
        typeC.specificRequest();
    }
}


// ? Adaptee
class TypeC {
    public void specificRequest() {
        System.out.println("TypeC数据线");
    }
}
```

![](./img/Snipaste_2025-10-16_15-38-47.png)

## 桥接模式(4次考过)

![](./img/Snipaste_2025-10-16_15-50-34.png)

![](./img/Snipaste_2025-10-16_15-51-00.png)

```java
public class Client {
    public static void main(String[] args) {
        Color red = new Red();
        Color blue = new Blue();

        Product productAR = new ProductA("产品A", red);
        Product productAB = new ProductA("产品A", blue);

        productAR.operation();
        productAB.operation();
    }
}

public interface Color {
    void operationImpl(String product);
}

class Red implements Color{
    @Override
    public void operationImpl(String product) {
        System.out.println(product + " 红色");
    }
}

class Blue implements Color{
    @Override
    public void operationImpl(String product) {
        System.out.println(product + " 蓝色");
    }
}

public abstract class Product {
    protected String name;
    protected Color color; // ! 持有颜色的引用（桥接）

    public Product(String name, Color color) {
        this.name = name;
        this.color = color;
    }

    abstract void operation();
}

class ProductA extends Product {
    public ProductA(String name, Color color) {
        super(name, color);
    }

    @Override
    void operation() {
        color.operationImpl(this.name);
    }
}
```

![](./img/Snipaste_2025-10-16_16-08-12.png)

![](./img/Snipaste_2025-10-16_16-09-41.png)

## 组合模式(3次考过)，考

![](./img/Snipaste_2025-10-16_16-38-56.png)

```java
public class Client {
    public static void main(String[] args) {
        FileComponent file1 = new FileLeaf("文件1");
        FileComponent file2 = new FileLeaf("文件2");
        FileComponent file3 = new FileLeaf("文件3");

        FileComponent root = new FolderComposite("根文件夹");
        FileComponent folder1 = new FolderComposite("文件夹1");

        folder1.add(file2);
        folder1.add(file3);

        root.add(file1);
        root.add(folder1);

        root.operation("");
    }
}

public abstract class FileComponent {
    public FileComponent(String name) {
        this.name = name;
    }

    String name;

    abstract void operation(String prefix);
    abstract void add(FileComponent component);
    abstract void remove(FileComponent component);
}

class FileLeaf extends FileComponent {
    public FileLeaf(String name) {
        super(name);
    }

    @Override
    void operation(String prefix) {
        System.out.println(prefix + "文件名:" + name);
    }

    @Override
    void add(FileComponent component) {
        return;
    }

    @Override
    void remove(FileComponent component) {
        return;
    }
}

class FolderComposite extends FileComponent{
    private List<FileComponent> children = new ArrayList<>();

    public FolderComposite(String name) {
        super(name);
    }

    @Override
    void operation(String prefix) {
        System.out.println(prefix + "文件夹:" + name);
        getChild(prefix);
    }

    @Override
    public void add(FileComponent fileComponent) {
        children.add(fileComponent);
    }

    @Override
    public void remove(FileComponent fileComponent) {
        children.remove(fileComponent);
    }

    public void getChild(String prefix) {
        for (FileComponent component : children) {
            component.operation(prefix + " ");
        }
    }
}
```

![](./img/Snipaste_2025-10-16_17-06-08.png)


## 装饰器模式(2次考过)，考

![](./img/Snipaste_2025-10-16_18-36-13.png)

```java
public class Client {
    public static void main(String[] args) {
        // ? 第一种
        PersonComponent zhangsan = new StudentConcreteComponent("张三");
        zhangsan = new ConcreteDecoratorA(zhangsan);
        zhangsan = new ConcreteDecoratorB(zhangsan);
        zhangsan.operation();

        System.out.println();
        System.out.println("================");

        // ? 第二种
        PersonComponent lisi = new ConcreteDecoratorB(
                new ConcreteDecoratorA(
                        new StudentConcreteComponent("李四")
                )
        );
        lisi.operation();
    }
}

public abstract class PersonComponent {
    String name;
    abstract void operation();
}

class StudentConcreteComponent extends PersonComponent {
    public StudentConcreteComponent(String name) {
        this.name = name;
    }

    @Override
    void operation() {
        System.out.print("学生上课 ");
    }
}

public abstract class Decorator extends PersonComponent {
    PersonComponent personComponent;
}

class ConcreteDecoratorA extends Decorator{
    public ConcreteDecoratorA(PersonComponent personComponent) {
        this.personComponent = personComponent;
    }

    @Override
    void operation() {
        personComponent.operation();
        System.out.print("做作业 ");
    }
}

class ConcreteDecoratorB extends Decorator{
    public ConcreteDecoratorB(PersonComponent personComponent) {
        this.personComponent = personComponent;
    }

    @Override
    void operation() {
        personComponent.operation();
        System.out.print("考试 ");
    }
}
```

![](./img/Snipaste_2025-10-16_19-35-08.png)

## 外观模式(没考过)

![](./img/Snipaste_2025-10-16_19-37-48.png)

```java
public class Client {
    public static void main(String[] args) {
        Facade facade = new Facade();
        facade.execute();
    }
}

public class Facade {
    private SubSystem1 subSystem1;
    private SubSystem2 subSystem2;
    private SubSystem3 subSystem3;

    public Facade() {
        this.subSystem1 = new SubSystem1();
        this.subSystem2 = new SubSystem2();
        this.subSystem3 = new SubSystem3();
    }

    public void execute() {
        subSystem1.method1();
        subSystem2.method2();
        subSystem3.method3();
    }
}

class SubSystem1 {
    public void method1() {
        System.out.println("子系统1");
    }
}

class SubSystem2 {
    public void method2() {
        System.out.println("子系统2");
    }
}

class SubSystem3 {
    public void method3() {
        System.out.println("子系统3");
    }
}
```

![](./img/Snipaste_2025-10-16_19-45-55.png)

![](./img/Snipaste_2025-10-16_19-47-08.png)

## 享元模式(1次考过)

![](./img/Snipaste_2025-10-16_19-53-32.png)

![](./img/Snipaste_2025-10-16_19-56-44.png)

```java
public class Client {
    public static void main(String[] args) {
        PieceFactory pieceFactory = new PieceFactory();

        Piece whitePiece1 = pieceFactory.getPiece(0);
        Piece whitePiece2 = pieceFactory.getPiece(0);

        System.out.println(whitePiece1 == whitePiece2);
    }
}

public class PieceFactory {
    Piece[] pieces = new Piece[]{new WhitePiece(), new BlackPiece()};

    public Piece getPiece(int key) {
        if (key == 0) return pieces[0];
        else return pieces[1];
    }
}

public abstract class Piece {
    abstract void draw(int x, int y);
}

class WhitePiece extends Piece{
    @Override
    void draw(int x, int y) {
        System.out.println("白色棋子，坐标：" + x + " " + y);
    }
}

class BlackPiece extends Piece{
    @Override
    void draw(int x, int y) {
        System.out.println("黑色棋子，坐标：" + x + " " + y);
    }
}
```

![](./img/Snipaste_2025-10-16_21-00-44.png)

## 代理模式(没考过)

![](./img/Snipaste_2025-10-16_21-14-15.png)

```java
public class Client {
    public static void main(String[] args) {
        RealSubject realSubject = new RealSubject();
        Proxy proxy = new Proxy(realSubject);

        proxy.buy();
    }
}

class Proxy implements Subject{
    RealSubject realSubject;

    public Proxy(RealSubject realSubject) {
        this.realSubject = realSubject;
    }

    @Override
    public void buy() {
        System.out.println("购买前的手续");
        realSubject.buy();
        System.out.println("购买后的手续");
    }
}

public interface Subject {
    void buy();
}

class RealSubject implements Subject{
    @Override
    public void buy() {
        System.out.println("买东西");
    }
}
```

![](./img/Snipaste_2025-10-16_21-16-37.png)

# 行为型模式

## 责任链模式(没考过)，考

![](./img/Snipaste_2025-10-17_11-39-53.png)

```java
public class Client {
    public static void main(String[] args) {
        // ! 构成责任链：辅导员 -> 院长 -> 校长
        Handler headMaster = new HeadMaster(null);
        Handler dean = new Dean(headMaster);
        Handler instructor = new Instructor(dean);

        instructor.handleRequest(30);
    }
}

public abstract class Handler {
    Handler next;

    public Handler(Handler next) {
        this.next = next;
    }

    public abstract void handleRequest(int request);
}

class Instructor extends Handler {
    public Instructor(Handler next) {
        super(next);
    }

    @Override
    public void handleRequest(int request) {
        if (request <= 7) {
            System.out.println("辅导员审批通过...");
        } else if (next != null) {
            next.handleRequest(request);
        } else {
            System.out.println("无法审批通过");
        }
    }
}

class Dean extends Handler {
    public Dean(Handler next) {
        super(next);
    }

    @Override
    public void handleRequest(int request) {
        if (request <= 15) {
            System.out.println("院长审批通过...");
        } else if (next != null) {
            next.handleRequest(request);
        } else {
            System.out.println("无法审批通过");
        }
    }
}

class HeadMaster extends Handler {
    public HeadMaster(Handler next) {
        super(next);
    }

    @Override
    public void handleRequest(int request) {
        if (request <= 30) {
            System.out.println("校长审批通过...");
        } else if (next != null) {
            next.handleRequest(request);
        } else {
            System.out.println("无法审批通过");
        }
    }
}
```

## 命令模式(1次考过)，考

![](./img/Snipaste_2025-10-17_12-31-28.png)

![](./img/Snipaste_2025-10-17_12-43-37.png)

```java
public class Client {
    public static void main(String[] args) {
        TvReceiver tv = new TvReceiver();

        Command onCommand = new OnCommand(tv);
        Command offCommand = new OffCommand(tv);

        RemoteControlInvoker remoteControl = new RemoteControlInvoker();
        remoteControl.setCommand(onCommand);
        remoteControl.execute();
        remoteControl.undo();

        System.out.println("======================");

        remoteControl.setCommand(offCommand);
        remoteControl.execute();
        remoteControl.undo();
    }
}

public class TvReceiver {
    public void onAction() {
        System.out.println("打开电视...");
    }

    public void offAction() {
        System.out.println("关闭电视...");
    }
}

public interface Command {
    void execute();
    void undo();
}

class OnCommand implements Command {
    TvReceiver tv;

    public OnCommand(TvReceiver tv) {
        this.tv = tv;
    }

    @Override
    public void execute() {
        tv.onAction();
    }

    @Override
    public void undo() {
        tv.offAction();
    }
}

class OffCommand implements Command {
    TvReceiver tv;

    public OffCommand(TvReceiver tv) {
        this.tv = tv;
    }

    @Override
    public void execute() {
        tv.offAction();
    }

    @Override
    public void undo() {
        tv.onAction();
    }
}

public class RemoteControlInvoker {
    Command command;

    public void setCommand(Command command) {
        this.command = command;
    }

    public void execute() {
        command.execute();
    }

    public void undo() {
        command.undo();
    }
}
```

![](./img/Snipaste_2025-10-17_12-50-29.png)

![](./img/Snipaste_2025-10-17_12-52-35.png)

![](./img/Snipaste_2025-10-17_12-54-36.png)

## 解释器模式(没考过)(类)

![](./img/Snipaste_2025-10-17_14-48-01.png)

![](./img/Snipaste_2025-10-17_15-00-44.png)

```java
public class Client {
    public static void main(String[] args) {
        Context context = new Context();
        context.check("A的zs");
        context.check("B的ls");
    }
}

public interface Expression {
    boolean interpret(String info);
}

class NoTerminalExpression implements Expression {
    Expression region;
    Expression person;

    public NoTerminalExpression(Expression region, Expression person) {
        this.region = region;
        this.person = person;
    }

    @Override
    public boolean interpret(String info) {
        String[] split = info.split("的");
        return region.interpret(split[0]) && person.interpret(split[1]);
    }
}

class TerminalExpression implements Expression{
    Set<String> set = new HashSet<>();

    public TerminalExpression(String[] data) {
        set.addAll(Arrays.asList(data));
    }

    @Override
    public boolean interpret(String info) {
        return set.contains(info);
    }
}

public class Context {
    private String[] regions = {"A", "B", "C"};
    private String[] persons = {"zs", "ls", "ww"};

    private final Expression noTerminalExpression;

    public Context() {
        Expression region = new TerminalExpression(regions);
        Expression person = new TerminalExpression(persons);
        noTerminalExpression = new NoTerminalExpression(region, person);
    }

    public void check(String info) {
        boolean res = noTerminalExpression.interpret(info);
        if (res) System.out.println("识别成功");
        else System.out.println("识别失败");
    }
}
```

## 迭代器模式(1次考过)

![](./img/Snipaste_2025-10-17_15-26-57.png)

![](./img/Snipaste_2025-10-17_15-33-08.png)

```java
public class Client {
    public static void main(String[] args) {
        BookAggregate bookAggregate = new BookAggregate();
        bookAggregate.add(new Book("1"));
        bookAggregate.add(new Book("2"));
        bookAggregate.add(new Book("3"));

        Iterator iterator = bookAggregate.createIterator();
        while (iterator.hasNext()) {
            Book book = (Book) iterator.next();
            System.out.println(book.getName());
        }
    }
}

public class Book {
    private String name;

    public Book(String name) {
        this.name = name;
    }

    public String getName() {
        return name;
    }
}

public interface Aggregate {
    Iterator createIterator();
}

class BookAggregate implements Aggregate{
    List<Book> books = new ArrayList<>();

    public void add(Book book) {
        books.add(book);
    }

    public Book get(int index) {
        return books.get(index);
    }

    public int size() {
        return books.size();
    }

    @Override
    public Iterator createIterator() {
        return new BookIterator(this);
    }
}

public interface Iterator {
    Boolean hasNext();
    Object next();
}

class BookIterator implements Iterator{
    private final BookAggregate bookAggregate;
    private int index;

    public BookIterator(BookAggregate aggregate) {
        this.bookAggregate = aggregate;
    }

    @Override
    public Boolean hasNext() {
        return index < bookAggregate.size();
    }

    @Override
    public Object next() {
        Object book = bookAggregate.get(index);
        index++;
        return book;
    }
}
```

## 中介者模式(1次考过)，考

![](./img/Snipaste_2025-10-17_22-24-38.png)

![](./img/Snipaste_2025-10-17_22-19-19.png)

```java
public class Client {
    public static void main(String[] args) {
        User zs = new Colleague("zs");
        User ls = new Colleague("ls");
        User ww = new Colleague("ww");

        ChatMediator chatMediator = new ChatConcreteMediator();
        chatMediator.registerMessage(zs);
        chatMediator.registerMessage(ls);
        chatMediator.registerMessage(ww);

        zs.sendMessage("你好");
    }
}

public abstract class User {
    private final String name;
    protected ChatMediator chatMediator;

    public User(String name) {
        this.name = name;
    }

    public String getName() {
        return name;
    }

    public void setChatMediator(ChatMediator chatMediator) {
        this.chatMediator = chatMediator;
    }

    abstract void sendMessage(String message);
    abstract void receiveMessage(String sender, String message);
}

class Colleague extends User {
    public Colleague(String name) {
        super(name);
    }

    @Override
    public void sendMessage(String message) {
        System.out.println(this.getName() + "发送消息：" + message);
        chatMediator.sendMessage(message, this);
    }

    @Override
    public void receiveMessage(String sender, String message) {
        System.out.println(this.getName() + "收到来自" + sender + "的消息:" + message);
    }
}

public interface ChatMediator {
    void sendMessage(String message, User sender);
    void registerMessage(User user);
}

class ChatConcreteMediator implements ChatMediator{
    private Map<String, User> users;

    public ChatConcreteMediator() {
        users = new HashMap<>();
    }

    @Override
    public void sendMessage(String message, User sender) {
        for (User user: users.values()) {
            if (!user.equals(sender)) {
                user.receiveMessage(sender.getName(), message);
            }
        }
    }

    @Override
    public void registerMessage(User user) {
        users.put(user.getName(), user);
        user.setChatMediator(this);
    }
}
```

![](./img/Snipaste_2025-10-17_22-41-06.png)

![](./img/Snipaste_2025-10-17_22-41-40.png)

## 备忘录模式(1次考过)

![](./img/Snipaste_2025-10-18_10-34-52.png)

![](./img/Snipaste_2025-10-18_10-45-01.png)

```java
public class Client {
    public static void main(String[] args) {
        Caretaker caretaker = new Caretaker();

        Originator originator = new Originator();
        originator.setState("1024");
        caretaker.saveState(originator.createMemento());

        originator.setState("2048");
        caretaker.saveState(originator.createMemento());

        originator.setState("4096");
        caretaker.saveState(originator.createMemento());

        caretaker.showState();

        Memento memento = caretaker.getState(2);
        originator.restoreFromMemento(memento);

        System.out.println(originator.getState());
    }
}

public class Memento {
    private String state;

    public Memento(String state) {
        this.state = state;
    }

    public String getState() {
        return state;
    }
}

public class Originator {
    private String state;

    public String getState() {
        return state;
    }

    public void setState(String state) {
        this.state = state;
    }

    public void restoreFromMemento(Memento memento) {
        this.state = memento.getState();
    }

    public Memento createMemento() {
        return new Memento(this.state);
    }
}

public class Caretaker {
    private List<Memento> mementoList;

    public Caretaker() {
        this.mementoList = new ArrayList<>();
    }

    public void saveState(Memento memento) {
        mementoList.add(memento);
    }

    public Memento getState(int index) {
        if (index >= 1 && index < mementoList.size()) {
            return mementoList.get(index - 1);
        }
        return null;
    }

    public void showState() {
        int cnt = 1;
        for (Memento memento: mementoList) {
            System.out.println("第" + cnt + "次" + "备份：" + memento.getState());
            cnt++;
        }
    }
}
```

## 观察者模式(2次考过)，考

![](./img/Snipaste_2025-10-18_11-08-27.png)

![](./img/Snipaste_2025-10-18_11-28-11.png)

```java
public class Client {
    public static void main(String[] args) {
        Subject subject = new UpSubject();

        Observer zs = new UserObserver("zs", subject);
        Observer ls = new UserObserver("ls", subject);
        Observer ww = new UserObserver("ww", subject);

        subject.setState("更新了");

        System.out.println("================");

        subject.setState("断更了");
    }
}

public interface Subject {
    void attach(Observer observer);
    void detach(Observer observer);
    void inform();

    String getState();
    void setState(String state);
}

class UpSubject implements Subject{
    private String state;

    private List<Observer> observerList;

    public UpSubject() {
        state = "无状态";
        this.observerList = new ArrayList<>();
    }

    @Override
    public void attach(Observer observer) {
        observerList.add(observer);
    }

    @Override
    public void detach(Observer observer) {
        observerList.remove(observer);
    }

    @Override
    public void inform() {
        for (Observer observer: observerList) {
            observer.update();
        }
    }

    @Override
    public String getState() {
        return this.state;
    }

    @Override
    public void setState(String state) {
        this.state = state;
        inform();
    }
}

public interface Observer {
    void update();
}

class UserObserver implements Observer{
    private final String name;
    private final Subject subject;

    private String state;

    public UserObserver(String name, Subject subject) {
        this.name = name;
        this.subject = subject;
        subject.attach(this);
        state = subject.getState();
    }

    @Override
    public void update() {
        System.out.println(name + "收到通知");
        this.state = subject.getState();
        System.out.println(this.name + "订阅的up状态更新了：" + state);
    }
}
```

![](./img/Snipaste_2025-10-18_11-48-48.png)

![](./img/Snipaste_2025-10-18_11-51-13.png)

![](./img/Snipaste_2025-10-18_11-51-29.png)

![](./img/Snipaste_2025-10-18_11-52-11.png)

## 状态模式(2次考过)

![](./img/Snipaste_2025-10-18_14-59-39.png)

```java
public class Client {
    public static void main(String[] args) {
        Context context = new Context(2);

        System.out.println("当前状态：" + context.getState().getName());

        context.request(); // 1
        context.request(); // 0

        context.request(); // 失败
        System.out.println("当前状态：" + context.getState().getName());
        context.request();
    }
}

public interface State {
    String getName();
    void handle(Context context);
}

class StateA implements State{
    @Override
    public String getName() {
        return "有货";
    }

    @Override
    public void handle(Context context) {
        int cnt = context.getCnt();
        if (cnt >= 1) {
            System.out.println("购买成功");
            context.setCnt(cnt-- - 1);
            if (cnt == 0)
                context.setState(Context.SB);
        } else {
            System.out.println("购买失败");
        }
    }
}

class StateB implements State {
    @Override
    public String getName() {
        return "无货";
    }

    @Override
    public void handle(Context context) {
        System.out.println("当前状态为无货，请等待补货");
        context.setCnt(5);
        context.setState(Context.SA);
        System.out.println("补货成功...");
    }
}

public class Context {
    public static final State SA = new StateA();
    public static final State SB = new StateB();

    private int cnt;
    private State state;

    public Context(int cnt) {
        this.cnt = cnt;
        state = SA;
    }

    public State getState() {
        return state;
    }

    public void setState(State state) {
        this.state = state;
    }

    public int getCnt() {
        return cnt;
    }

    public void setCnt(int cnt) {
        this.cnt = cnt;
    }

    public void request() {
        state.handle(this);
    }
}
```

![](./img/Snipaste_2025-10-18_15-20-35.png)

![](./img/Snipaste_2025-10-18_15-21-55.png)
![](./img/Snipaste_2025-10-18_15-22-12.png)

## 策略模式(3次考过)

![](./img/Snipaste_2025-10-18_15-41-42.png)

![](./img/Snipaste_2025-10-18_15-40-36.png)

```java
public class Client {
    public static void main(String[] args) {
        Strategy wechat = new WechatPayStrategy();
        Strategy aliPay = new AliPayStrategy();

        Context context = new Context();
        context.setStrategy(wechat);

        context.pay(100);
        context.setStrategy(aliPay);
        context.pay(200);
    }
}

public class Context {
    Strategy strategy;

    public Context() {
        strategy = new AliPayStrategy();
    }

    public void setStrategy(Strategy strategy) {
        this.strategy = strategy;
    }

    public void pay(int money) {
        strategy.pay(money);
    }
}

public interface Strategy {
    String getPayName();
    void pay(int money);
}

class AliPayStrategy implements Strategy {
    @Override
    public String getPayName() {
        return "支付宝支付";
    }

    @Override
    public void pay(int money) {
        System.out.println("使用" + getPayName() + "支付" + money + "元");
    }
}

class WechatPayStrategy implements Strategy {
    @Override
    public String getPayName() {
        return "微信支付";
    }

    @Override
    public void pay(int money) {
        System.out.println("使用" + getPayName() + "支付" + money + "元");
    }
}
```

![](./img/Snipaste_2025-10-18_15-43-57.png)

## 模板方法模式(没考过)(类)

![](./img/Snipaste_2025-10-18_19-46-24.png)

![](./img/Snipaste_2025-10-18_19-47-54.png)

```java
public class Client {
    public static void main(String[] args) {
        Person student = new Student();
        student.templateMethod();

        System.out.println("=====================");

        Person teacher = new Teacher();
        teacher.templateMethod();
    }
}

public abstract class Person {
    public void templateMethod() {
        System.out.println("上课进教室...");
        primitiveOperation1();
        System.out.println("下课出教室");
        primitiveOperation2();
    }

    protected abstract void primitiveOperation1();
    protected abstract void primitiveOperation2();
}

class Student extends Person {
    @Override
    protected void primitiveOperation1() {
        System.out.println("学生：听讲，记笔记，回答问题");
    }

    @Override
    protected void primitiveOperation2() {
        System.out.println("学生：回家，写作业，复习知识");
    }
}

class Teacher extends Person {
    @Override
    protected void primitiveOperation1() {
        System.out.println("教师：讲课，写板书，提问学生");
    }

    @Override
    protected void primitiveOperation2() {
        System.out.println("教师：备课，改作业，查看情况");
    }
}
```

## 访问者模式(3次考过)

![](./img/Snipaste_2025-10-18_20-22-32.png)

![](./img/Snipaste_2025-10-18_20-23-25.png)

![](./img/Snipaste_2025-10-18_20-24-11.png)

```java
public class Client {
    public static void main(String[] args) {
        ObjStructure objStructure = new ObjStructure();

        AgeSumVisitor visitor1 = new AgeSumVisitor();
        NameVisitor visitor2 = new NameVisitor();

        objStructure.accept(visitor1);
        System.out.println("教师的平均年龄：" + visitor1.getAvgAgeTea() + " 学生的平均年龄：" + visitor1.getAvgAgeStu());
        System.out.println("======================");
        objStructure.accept(visitor2);
        System.out.println(visitor2.getNameSumTea() + " " + visitor2.getNameSumStu());
    }
}

public abstract class Person {
    private final String name;
    private final int age;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    public String getName() {
        return name;
    }

    public int getAge() {
        return age;
    }

    public abstract void accept(Visitor visitor);
}

class Student extends Person {
    public Student(String name, int age) {
        super(name, age);
    }

    @Override
    public void accept(Visitor visitor) {
        System.out.println("访问者访问了学生：" + getName());
        visitor.visitStudent(this);
    }
}

class Teacher extends Person {
    public Teacher(String name, int age) {
        super(name, age);
    }

    @Override
    public void accept(Visitor visitor) {
        System.out.println("访问者访问了教师：" + getName());
        visitor.visitTeacher(this);
    }
}

public interface Visitor {
    void visitStudent(Student student);
    void visitTeacher(Teacher teacher);
}

class AgeSumVisitor implements Visitor {
    private double avgAgeStu;
    private double avgAgeTea;

    @Override
    public void visitStudent(Student student) {
        avgAgeStu += student.getAge();
    }

    @Override
    public void visitTeacher(Teacher teacher) {
        avgAgeTea += teacher.getAge();
    }

    public double getAvgAgeStu() {
        return avgAgeStu / 3;
    }

    public double getAvgAgeTea() {
        return avgAgeTea / 2;
    }
}

class NameVisitor implements Visitor {
    private final StringBuilder nameSumStu = new StringBuilder();
    private final StringBuilder nameSumTea = new StringBuilder();

    @Override
    public void visitStudent(Student student) {
        nameSumStu.append(student.getName()).append(" ");
    }

    @Override
    public void visitTeacher(Teacher teacher) {
        nameSumTea.append(teacher.getName()).append(" ");
    }

    public String getNameSumStu() {
        return "学生姓名：" + nameSumStu;
    }

    public String getNameSumTea() {
        return "教师姓名：" + nameSumTea;
    }
}

public class ObjStructure {
    private final List<Person> people;

    public ObjStructure() {
        people = List.of(new Student("zs", 18), new Student("ls", 19), new Student("ww", 18),
                new Teacher("zl", 39), new Teacher("lq", 40));
    }

    public void accept(Visitor visitor) {
        for (Person person: people) {
            person.accept(visitor);
        }
    }
}
```

![](./img/Snipaste_2025-10-18_20-40-21.png)

![](./img/Snipaste_2025-10-18_20-43-28.png)

# 例题

![](./img/Snipaste_2025-10-18_21-08-46.png)

![](./img/Snipaste_2025-10-18_21-09-22.png)

![](./img/Snipaste_2025-10-18_21-14-19.png)

![](./img/Snipaste_2025-10-18_21-17-27.png)

