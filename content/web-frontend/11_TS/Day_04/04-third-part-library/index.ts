import axios from 'axios'
import * as _ from 'lodash'
import * as React from 'react'

// 添加一个简单的 react 使用示例
React.createElement('div', {
  className: 'container'
}, 'Hello, World!')


// 添加一个简单的 lodash 使用示例
_.isInteger(123)

axios({
  url: '',
  method: 'get'
})

